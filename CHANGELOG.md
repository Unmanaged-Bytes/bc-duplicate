# Changelog

## 1.0.0 (unreleased)

- Initial scaffold: meson project, debian packaging, CLI spec via
  bc-runtime-cli with `scan` and `summary` subcommands, application
  lifecycle wired through bc_runtime, first cmocka unit-test suite
  on the CLI binding layer.
